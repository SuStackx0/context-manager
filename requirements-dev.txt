# Lightweight install for running tests / the app skeleton WITHOUT the heavy
# local-model stack (torch, transformers, sentence-transformers).
# Tests use fake providers, so none of the model libraries are needed.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
python-dotenv==1.0.1
faiss-cpu==1.9.0.post1
numpy==2.2.1
pytest==8.3.4
httpx==0.28.1
